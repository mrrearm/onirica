<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Il permesso INTERNET serve SOLO se l'utente attiva volontariamente
        l'interpretazione AI (opzionale, disattivata di default) nelle
        Impostazioni, inserendo una propria chiave API Groq gratuita.
        Senza questa attivazione l'app resta completamente offline e usa
        l'interprete locale basato su regole.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Onirica"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Onirica">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
